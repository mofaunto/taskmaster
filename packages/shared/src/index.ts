// Types and schemas shared by the mobile app and the API.
// Filled in as the backend takes shape; keeping it empty for now would make
// the workspace wiring untestable.
export const SHARED_PACKAGE_NAME = "@taskmaster/shared";
